# Required: used for fact extraction (gpt-4o-mini) and embeddings (text-embedding-3-small)
OPENAI_API_KEY=sk-...

# Optional: if set, all endpoints except /health require "Authorization: Bearer <token>"
MEMORY_AUTH_TOKEN=
